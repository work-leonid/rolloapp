<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: resource.data.title %>
  </head>
  <body class="text-xl text-slate-800">

    <main class="p-10 lg:p-20 bg-gradient-to-bl from-purple-300 via-rose-200 to-sky-200">
      <%= yield %>
    </main>

  </body>
</html>
